node_modules/
dist/
*.db
*.db-journal
data/
.env
.DS_Store
coverage/
*.tsbuildinfo

# Playwright run artifacts — regenerated by every `npm run test:e2e`
test-results/
playwright-report/

# Agent worktrees — checkouts of branches that already live in git
.claude/worktrees/

# Investigator evidence — local working notes and repro tests. The durable
# record of anything worth keeping is the GitHub issue the investigation filed.
.claude/investigations/
